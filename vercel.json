{
  "buildCommand": "pnpm --filter frontend build",
  "outputDirectory": "apps/frontend/dist",
  "installCommand": "pnpm install",
  "framework": null,
  "ignoreCommand": "git diff HEAD^ HEAD --quiet -- apps/frontend vercel.json",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://wingertmap.onrender.com/api/:path*"
    }
  ]
}
